# Analyzer_TsumTsum

Analyzer_TsumTsum 用の PySide6 デスクトップアプリです。

## セットアップ

```bash
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -U pip setuptools wheel
python3 -m pip install --no-compile -e .
```

## 起動

```bash
python3 -m app.main
```

または

```bash
analyzer-tsumtsum
```
